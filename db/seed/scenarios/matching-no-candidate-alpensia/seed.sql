-- FLOW preparation only. Idle base already has no exposed instructor or active request.
-- The Alpensia request is created through REST, so this scenario has no SQL delta.
START TRANSACTION;
COMMIT;
